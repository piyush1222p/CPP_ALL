#include <iostream>
int main()
{
    std::string Name[2] = {"Piyush","Anant"};

    std::cout<<"The name of the First Student is "<<Name[0]<<std::endl;
    std::cout<<"The name of the Second Student is "<<Name[1]<<std::endl;

    return 0;
}
/* We can create an array and after that we can assign the values to them by using index number of the array. 
   We can create an array and after that we can access the their values by using their index number .*/
